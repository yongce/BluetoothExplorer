buildscript {
    ext {
        androidProjectCommon = "${rootProject.projectDir}/android_project_common.gradle"
        androidModuleCommon = "${rootProject.projectDir}/android_module_common.gradle"
    }
    apply from: "${androidProjectCommon}"

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.1'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${versions.kotlin}"
        classpath "com.jakewharton:butterknife-gradle-plugin:${versions.butterknife}"

        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1'
        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.8.4'
    }
}

// Add plugin for 'spotless'
plugins {
    id "com.diffplug.gradle.spotless" version "3.16.0"
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

clean {
    delete rootProject.buildDir
}

spotless {
    kotlin {
        target "**/*.kt"
        ktlint(versions.ktlint)
    }
}

ext {
    // For bintray upload
    bintrayMaven = [
            'projectRepo': 'android',
            'projectUrl': 'https://github.com/yongce/BleExplore',
            'projectScmConnection': 'https://github.com/yongce/BleExplore.git',
            'projectScmDevConnection': 'ssh://git@github.com/yongce/BleExplore.git',
            'projectInceptionYear': '2018',
            'groupId': 'me.ycdev.android',
            'version': '1.1.2',
            'developerId': 'yongce',
            'developerName': 'Yongce Tu',
            'developerEmail': 'yongce.tu@gmail.com',
    ]
}
